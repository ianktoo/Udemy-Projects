import random
from students.UniversityUnits import MyMajor

# generate a random integer

generated_random = random.randint(1,10) # random integer between 1 and 10

print(f'The type of generated random is {type(generated_random)} and the value is {generated_random}')

